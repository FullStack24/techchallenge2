name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Set up Docker
        uses: docker/setup-docker@v2

      - name: Build Docker image
        run: docker build -t my-app:latest .

      - name: Run Prisma Migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_DOCKER }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: docker run --rm --env DATABASE_URL=${{ secrets.DATABASE_URL_DOCKER }} --env JWT_SECRET=${{ secrets.JWT_SECRET }} my-app:latest npx prisma migrate deploy

      - name: Run Tests
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_DOCKER }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: docker run --rm --env DATABASE_URL=${{ secrets.DATABASE_URL_DOCKER }} --env JWT_SECRET=${{ secrets.JWT_SECRET }} my-app:latest npm test

      - name: Deploy to Production
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_DOCKER }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: |
          docker run -d -p 3000:3000 --env DATABASE_URL=${{ secrets.DATABASE_URL_DOCKER }} --env JWT_SECRET=${{ secrets.JWT_SECRET }} my-app:latest
